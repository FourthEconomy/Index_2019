# COLUMBUS, GA

library(shiny)
library(shinythemes)
library(leaflet)
library(rgdal)
library(rsconnect)
library(scales)
library(shinyalert)
library(plotly)
library(googlesheets)

##LOADING IN DATA

# load(file = "indexdata.rda")

# File should include:
#   scores.df
#   raw_values.df
#   text_strings.df
  


##DEFINING FUNCTIONS

server <- function(input, output, session) {

  
  
}
